============
Installation
============

At the command line::

    $ easy_install frame_logging

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv frame_logging
    $ pip install frame_logging
